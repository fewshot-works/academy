[project]
name = "capstone"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv",
    "requests",
    "chromadb",
    "langchain",
    "langchain-ollama",
    "langchain-openai",
    "langgraph",
    "streamlit>=1.61.1",
]
