[project]
name = "human-in-the-loop"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv",
    "requests",
    "langchain",
    "langgraph",
    "langchain-ollama",
    "langchain-openai",
    "langchain-anthropic",
]
