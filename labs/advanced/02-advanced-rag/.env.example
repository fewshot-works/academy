# Which AI provider to use: ollama or openai
# This lab needs one provider that can do both embeddings and chat -- Anthropic
# doesn't offer an embeddings API, so PROVIDER=anthropic isn't supported here.
PROVIDER=ollama

# Only needed if PROVIDER=openai
# OPENAI_API_KEY=sk-...
