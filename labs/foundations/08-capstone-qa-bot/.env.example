# Which AI provider to use: ollama or openai
# (this lab needs one provider that handles both embeddings and chat, and
# Anthropic doesn't offer an embeddings API, so it isn't supported here)
PROVIDER=ollama

# Only needed if PROVIDER=openai
# OPENAI_API_KEY=sk-...
