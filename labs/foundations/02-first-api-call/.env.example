# Choose which AI provider to use: ollama, openai, or anthropic
PROVIDER=ollama

# Only needed if PROVIDER=openai
# OPENAI_API_KEY=sk-...

# Only needed if PROVIDER=anthropic
# ANTHROPIC_API_KEY=sk-ant-...
