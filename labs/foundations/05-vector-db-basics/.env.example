# Which AI provider to use: ollama or openai
# (Anthropic doesn't offer an embeddings API, so it isn't supported in this lab)
PROVIDER=ollama

# Only needed if PROVIDER=openai
# OPENAI_API_KEY=sk-...
