[project]
name = "mcp-security"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv",
    "langchain",
    "langchain-ollama",
    "langchain-openai",
    "langchain-anthropic",
    "langchain-mcp-adapters",
    "mcp",
]
