[project]
name = "mcp-a2a"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv",
    "langchain",
    "langchain-ollama",
    "langchain-openai",
    "langchain-anthropic",
    "a2a-sdk[http-server]",
    "requests",
    "uvicorn",
]
