import type {ReactNode} from 'react';
import CareerTrackStubPage from '@site/src/components/CareerTrackStubPage';

export default function AiProductManager(): ReactNode {
  return (
    <CareerTrackStubPage
      title="AI Product Manager"
      oneLiner="Decides what gets built, not how it's built. A non-coding, decision-making track — approachable even with zero prior technical background."
    />
  );
}
