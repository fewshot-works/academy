import type {ReactNode} from 'react';
import CareerTrackStubPage from '@site/src/components/CareerTrackStubPage';

export default function ForwardDeployedEngineer(): ReactNode {
  return (
    <CareerTrackStubPage
      title="Forward-Deployed Engineer (FDE)"
      oneLiner="Embedded with a customer, shipping production AI inside their org. The deepest track: full curriculum plus judgment skills the labs alone don't teach."
    />
  );
}
